naftiko: 1.0.0-alpha2
info:
  label: commercetools Import API — StandalonePrices
  description: 'commercetools Import API — StandalonePrices. 1 operations. Lead operation: Import standalone prices. Self-contained
    Naftiko capability covering one Commercetools business surface.'
  tags:
  - Commercetools
  - StandalonePrices
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    COMMERCETOOLS_API_KEY: COMMERCETOOLS_API_KEY
capability:
  consumes:
  - type: http
    namespace: import-standaloneprices
    baseUri: https://import.{region}.commercetools.com
    description: commercetools Import API — StandalonePrices business capability. Self-contained, no shared references.
    resources:
    - name: projectKey-standalone-prices-import-containers-importContainerKey
      path: /{projectKey}/standalone-prices/import-containers/{importContainerKey}
      operations:
      - name: importstandaloneprices
        method: POST
        description: Import standalone prices
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.COMMERCETOOLS_API_KEY}}'
  exposes:
  - type: rest
    namespace: import-standaloneprices-rest
    port: 8080
    description: REST adapter for commercetools Import API — StandalonePrices. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/{projectkey}/standalone-prices/import-containers/{importcontainerkey}
      name: projectkey-standalone-prices-import-containers-importcontainerkey
      description: REST surface for projectKey-standalone-prices-import-containers-importContainerKey.
      operations:
      - method: POST
        name: importstandaloneprices
        description: Import standalone prices
        call: import-standaloneprices.importstandaloneprices
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: import-standaloneprices-mcp
    port: 9090
    transport: http
    description: MCP adapter for commercetools Import API — StandalonePrices. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: import-standalone-prices
      description: Import standalone prices
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: import-standaloneprices.importstandaloneprices
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
