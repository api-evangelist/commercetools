naftiko: 1.0.0-alpha2
info:
  label: commercetools Import API — Customers
  description: 'commercetools Import API — Customers. 1 operations. Lead operation: Import customers. Self-contained Naftiko
    capability covering one Commercetools business surface.'
  tags:
  - Commercetools
  - Customers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    COMMERCETOOLS_API_KEY: COMMERCETOOLS_API_KEY
capability:
  consumes:
  - type: http
    namespace: import-customers
    baseUri: https://import.{region}.commercetools.com
    description: commercetools Import API — Customers business capability. Self-contained, no shared references.
    resources:
    - name: projectKey-customers-import-containers-importContainerKey
      path: /{projectKey}/customers/import-containers/{importContainerKey}
      operations:
      - name: importcustomers
        method: POST
        description: Import customers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.COMMERCETOOLS_API_KEY}}'
  exposes:
  - type: rest
    namespace: import-customers-rest
    port: 8080
    description: REST adapter for commercetools Import API — Customers. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/{projectkey}/customers/import-containers/{importcontainerkey}
      name: projectkey-customers-import-containers-importcontainerkey
      description: REST surface for projectKey-customers-import-containers-importContainerKey.
      operations:
      - method: POST
        name: importcustomers
        description: Import customers
        call: import-customers.importcustomers
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: import-customers-mcp
    port: 9090
    transport: http
    description: MCP adapter for commercetools Import API — Customers. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: import-customers
      description: Import customers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: import-customers.importcustomers
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
